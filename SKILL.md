---
name: periodic-report-generator
description: 根据用户提供的项目进展、任务记录、数据、问题和计划，生成财务部中文周报与月报，并按需交付 HTML 或 PDF。适用于整理零散工作信息、汇总周期成果、说明风险与下阶段计划；不用于编造缺失业绩或替代财务审计。
---

# 周报与月报生成器

将原始信息整理成准确、易扫读、可直接发送的周期工作报告。保留用户给出的事实、数字、时间范围和专有名词，不夸大成果，也不把推测写成事实。

## 固定报告信息

每份报告必须在页面顶部清晰展示：

- 部门：财务部
- 日期：生成报告当天的本地日期，格式为 `YYYY年M月D日`
- 报告类型：周报或月报
- 报告周期：优先使用用户提供的统计起止日期；未提供时标注“待补充”

“日期”指实际生成当天，不得用报告周期结束日代替。应从当前运行环境读取当天日期，不把示例日期写死。

## 判断报告模式

- 用户明确要求周报或月报时，采用对应模式。
- 用户只说“工作报告”时，根据输入覆盖周期判断；仍无法判断，则先生成最合理版本，并简短说明采用的周期。
- 用户提供既有模板、栏目或示例时，优先沿用其结构和口吻。

## 整理输入

先从信息中识别：报告周期、受众、目标、已完成事项、量化结果、进行中事项、问题与风险、所需支持、下阶段计划。

- 合并重复事项，按项目、业务主题或优先级归组。
- 区分“已完成”“进行中”“计划中”，不得混写。
- 原始数据口径不一致或相互矛盾时，明确指出，不自行选择有利数字。
- 仅在缺失信息会实质改变报告时提问；否则用“待补充”“尚未提供”标记，并继续完成报告。
- 不要虚构数字、客户反馈、进度、原因、责任人、日期或结论。

## 写作要求

默认使用简洁、专业、结果导向的中文。优先呈现“事项 + 进展/结果 + 影响”，避免把工作流水账简单改写成完整句子。

### 周报

突出本周推进节奏和下一步执行，默认结构：

1. 本周概览
2. 重点工作与成果
3. 进行中事项
4. 问题、风险与所需支持
5. 下周计划

### 月报

突出月度目标、趋势、复盘和下月重点，默认结构：

1. 月度概览
2. 目标与核心指标完成情况
3. 重点工作与业务成果
4. 偏差、问题与经验复盘
5. 风险与所需支持
6. 下月重点计划

若输入包含足够的指标数据，可使用紧凑表格展示“指标、目标、实际、完成率/变化、说明”。只有在目标与实际口径一致时才计算完成率，并写清单位和统计周期。

## 输出前检查

- 时间范围、数字、单位、状态和项目名称与输入一致。
- 重要成果包含实际影响；无法确认影响时不补写。
- 风险说明影响和应对动作；未提供责任人或期限时不虚构。
- 下阶段计划尽量具体，但只使用用户已提供或可直接推出的动作。
- 删除重复、空泛表述；保证报告无需读者查看原始材料也能理解。

如用户需要更具体的栏目定义或输入模板，读取 [references/report-templates.md](references/report-templates.md)。

## 输出格式

用户可选择 HTML 或 PDF。若用户未指定，默认输出 HTML，并告知可继续转换为 PDF。

### HTML

- 生成一个可独立打开的 `.html` 文件，使用 UTF-8 编码。
- CSS 写入文件内部，不依赖外部字体、脚本、图片或网络资源。
- 页面适合桌面阅读和打印，使用清晰的标题层级、表格和留白。
- 添加打印样式：A4 纵向、合理页边距、避免标题与其内容跨页分离、避免表格行被截断。
- 不加入与报告无关的交互功能。

### PDF

- 先生成符合上述要求的 HTML，再将其渲染为 PDF，以保持两种格式内容一致。
- 使用可用的 PDF 工具完成渲染并检查最终页面；若当前环境提供 PDF 专用 skill，按其说明执行。
- 检查中文字体是否正常、表格是否越界、分页是否合理，以及顶部固定信息是否完整可见。
- 最终交付 `.pdf` 文件；如用户同时需要源文件，再一并交付 HTML。
