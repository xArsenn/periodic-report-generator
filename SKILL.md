---
name: professional-presentation
description: 把主题、文档、数据、图片或零散材料组织成清晰专业的单文件横向网页演示。适用于方案展示、主题分享、产品介绍、项目复盘、教学讲解、行业洞察和管理层沟通；周报与月报只是可选素材类型，不是该技能的默认用途。
---

# 专业展示生成器

把任何主题或材料转化为可离线打开、适合讲解和展示的单文件 HTML 演示。首要目标是让观众快速理解、持续关注并记住重点。采用专业咨询式的信息层级和克制配色，但不把所有内容强行写成月报、周报或经营分析。

## 展示工作流

开始前按需确认主题、观众、展示场景、时长、希望观众带走什么、现有材料与图片，以及必须包含或禁止出现的内容。随后读取：

1. [references/consulting-storyline.md](references/consulting-storyline.md)：搭建结论先行的逻辑线。
2. [references/content-clarity.md](references/content-clarity.md)：拆分材料、控制信息密度并规划页面节奏。
3. [references/layouts-consulting.md](references/layouts-consulting.md)：按内容形状选择母版。
4. [references/consulting-visual-system.md](references/consulting-visual-system.md)：执行专业配色和视觉规则。
5. [references/chart-components.md](references/chart-components.md)：需要图表时选择稳定的原生HTML/CSS组件。
6. [references/image-evidence.md](references/image-evidence.md)：需要截图、照片或单文件图片嵌入时执行。
7. [references/quality-checklist.md](references/quality-checklist.md)：生成后逐页验收。

执行步骤：

1. 识别材料中的主题、观点、事实、数字、故事、图片、步骤、对比、风险和待补信息。不得为了版式编造内容。
2. 先选择叙事模式：决策型、解释型、展示型或故事型；再写一句整份演示希望观众带走的核心信息。
3. 为每页写一个明确标题。决策型页面使用行动标题；解释型、产品展示和章节页可使用准确的主题标题或短句，不强制每页都写管理结论。
   封面只直接展示主题，不自动添加部门、报告期、生成日期或版本；这些字段仅在用户或场景需要时出现。
   正文页标题使用完整内容安全宽度，不手工插入换行；只在触及右侧安全边界后自然换行。封面、章节幕和收束页仅在存在明确语义分层时允许人工断行。
4. 建立页面计划：页码、页面目的、行动标题、证据、版式 ID、图片槽位和信息密度。先完成计划再写 HTML。
   同时标注预计主体高度与视觉重心。若一排模块只能占据上半页，必须在写HTML前改为纵向流程、2×2矩阵、中心主线或“主体 + 结论带”，不能把大片下半页留空。
5. 复制 `assets/template-consulting.html`。从 `assets/layout-gallery-consulting.html` 选择 C01—C16 完整示例，不从零发明结构。写页前确认所用类名已在模板中定义。
6. 保留键盘、滚轮、触屏、索引和 `prefers-reduced-motion` 支持；图片使用统一比例和明确的 `object-fit` 策略。
7. 输入为DOCX时可运行 `python scripts/extract-report-content.py <docx> --out <json>`；需要单文件嵌图时运行 `node scripts/embed-local-images.mjs <html> --out <html>`。这些是素材处理能力，不定义演示主题。
8. 运行 `node scripts/validate-consulting-deck.mjs <html>` 并修复所有错误。需要PDF时运行 `node scripts/export-html-pdf.mjs <html> --out <pdf>`，使用支持Playwright的Node运行时。
9. 在 1366×768 和 1920×1080 下逐页检查标题、溢出、底部安全区、文字大小和图片裁切。结构校验不能替代视觉验收。

## 展示与交付边界

- 事实与建议分开；未经证实的原因标为假设或待验证。
- 不把普通主题自动改写成经营汇报，不自动加入“执行摘要、部门、报告期、下月计划”等报告结构。
- 当材料确实是报告时，单日、部分主体或部分周期数据不得扩写成完整周期结论。
- 图表只展示真实可比较数据；没有数据时使用逻辑树、流程或文本结构，不制作伪图表。
- 单文件 HTML 可离线打开，无 TODO、占位符、重复页码或空白页。
- 每页只有一个核心信息；数据页的单位、范围和口径同屏可见。
- 同一语义只出现一次。页眉已有“执行摘要”等栏目名时，不再增加 `MANAGEMENT SUMMARY` 等同义英文眉题。
- 默认不在左下角显示来源、说明或生成信息等小字页脚。来源确有审计价值时，整合进表格标题、图片图注或单独来源页；只有用户明确要求时才使用来源页脚。
- 复杂内容优先拆页，不通过缩小字号或堆叠卡片硬塞；连续三页不得使用完全相同的主体结构。
- 用数字、关系、流程、状态、对比和证据图形成阅读变化；不把整份演示做成连续的“标题 + 三张文字卡片”。
- 当页面只有一句结论、一个数字或少量文字时，使用 `center-stage` 低密度版式：放大文字并水平、垂直居中，不用小卡片占据页面上半部。
- 留白必须围绕信息形成秩序，不能表现为“一排矮卡片 + 半页空白”。有效主体通常覆盖安全区60%—85%；不足时先换信息形态，禁止用空容器、装饰或无信息文字填充。
- 每页的列宽、模块高度、间距、连接线和文字基线保持一致；专业感来自清晰的比例和关系，不来自堆叠更多卡片。
- 页面顶部章节标签使用17—20px和清晰字重，必须明显大于页码等辅助信息，不能做成接近注脚大小的小字。
- 1366×768 与 1920×1080 下无溢出，导航不遮挡内容。
